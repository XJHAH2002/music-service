<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.music.mapper.MusicMapper">

    <!-- 查询所有音乐 -->
    <select id="findAll" resultType="Music">
        select * from music
    </select>

    <!-- 搜索音乐：歌名或歌手包含关键词 -->
    <select id="search" parameterType="String" resultType="Music">
        select * from music where title like concat('%', #{keyword}, '%') or singer like concat('%', #{keyword}, '%')
    </select>
</mapper>